# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on PR
on: pull_request
permissions:
  checks: write
  contents: read
  pull-requests: write
jobs:
  build_and_preview:
    if: ${{ github.event.pull_request.head.repo.full_name == github.repository }}
    runs-on: ubuntu-latest
    steps:name: Deploy to Firebase Hosting

on:
  push:
    branches:
      - master  # or the branch you want to deploy from

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '16'  # specify the version of Node.js you are using

      - name: Install dependencies
        run: |
          npm install

      - name: Build Next.js
        run: |
          npm run build

      - name: Firebase Deploy
        uses: shogun0322/portfolio
        with:
          firebase_token: ${{ secrets.FIREBASE_TOKEN }}  # Store Firebase token in GitHub Secrets
          project_id: YOUR_FIREBASE_PROJECT_ID  # Replace with your Firebase project ID

      - uses: actions/checkout@v4
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_SHO_PORTFOLIO_322 }}
          projectId: sho-portfolio-322
